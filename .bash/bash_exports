# Use vim as default editor
export EDITOR=vim

# Use an other defautl wine prefix
export WINEPREFIX=~/.wine/default

# TERM variable for color suppoert
export TERM=xterm-256color

# Load bash export files
for file in ~/.bash/exports/*.bash; do
    [ -r "$file" ] && source "$file"
done
