# This file is used to extend the PATH variable

PATH="$HOME/.bash/bin:$HOME/bin:$PATH"					# Set private bin path

# Load path files
for file in ~/.bash/path/*.bash; do
  [ -r "$file" ] && source "$file"
done

